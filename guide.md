# HTML 슬라이드 구성 지시서

이 문서는 HTML 슬라이드 제작을 위한 **내용·구조 지시서**입니다.
디자인(색·폰트·레이아웃·애니메이션)에 대한 지시는 포함하지 않습니다 — 디자인은 별도로 지정됩니다.

## 제작 공통 규칙

- 슬라이드는 총 19장. 순서·번호를 그대로 유지합니다.
- 각 슬라이드는 다음 요소로 구성됩니다: 제목 / 본문(불릿·표·수식) / 이미지.
- 수식은 일반 텍스트로 표기합니다(예: `f = 1 / T`).
- 표는 그대로 표로 만듭니다.
- 발표 대본(발표자 노트)은 이 지시서에 포함하지 않습니다. 화면에 보이는 내용만 담습니다.

## 이미지 처리 — 두 종류로 구분

각 슬라이드의 이미지 항목은 다음 셋 중 하나로 표시됩니다.

1. **[생성]** — Claude Code가 직접 SVG/도식 코드로 생성합니다. 음향학적 정밀도가 필요 없는 일반 도식(흐름도, 타임라인, 라인 스펙트럼, 곡선 등)입니다. 각 항목에 그릴 내용을 구체적으로 적어두었으니 그대로 코드로 구현합니다. 깔끔한 벡터 도식으로 만들고, 라벨 텍스트는 한국어로 넣습니다.
2. **[준비됨]** — 사용자가 직접 준비한 이미지 파일입니다. 음향학적 정확성이 중요한 도식과 음성 파일에서만 나오는 그림(Praat 스펙트로그램 캡처, pitch contour)이 여기 해당합니다. 파일은 준비되어 있다고 가정하고, `slideNN_설명.png` 형식의 placeholder로 자리만 잡습니다. 실제 경로는 제작 시 연결합니다. 만약 해당 파일이 폴더내에서 발견되지 않는다면 들어갈 공간을 비워둡니다.
3. **[데이터 그래프]** — 측정값으로 그리는 그래프(막대그래프, 산점도)입니다. Claude Code가 코드로 그립니다. 단, 지금은 실제 측정값이 없으므로 **음향학적으로 그럴듯한 더미값**을 넣어 그래프가 실제로 그려지게 합니다.

### [데이터 그래프] 작성 규칙 (슬라이드 13·14·15·16 공통)

- 모든 측정값(더미값)은 각 슬라이드 코드 **상단의 데이터 객체 한 곳에 모아** 정의하고, 바로 위에 `// TODO: 실제 측정값으로 교체` 주석을 단다.
- 같은 슬라이드의 **표와 그래프는 이 데이터 객체 하나에서 함께 채운다.** 표 셀에 숫자를 따로 하드코딩하지 않는다. (값을 한 곳만 바꾸면 표와 그래프가 동시에 갱신되도록.)
- 인간 = 한 색, TTS = 다른 색으로 통일한다(구체 색상은 디자인 단계에서 지정).
- 축 라벨·단위(ms, Hz)를 표시한다.

---

## 슬라이드 1 — 문제 제기

**제목:** AI 음성은 사람 목소리와 같은 방식으로 만들어질까?

**본문(불릿):**
- AI 보이스는 이미 일상에서 널리 쓰인다.
- 그러나 어떤 AI 음성은 발음이 정확해도 미묘하게 어색하게 들린다.
- 이 발표는 그 어색함을 인상이 아니라 음향 신호의 차이로 분석한다.

**이미지:** 없음(도입 슬라이드).

---

## 슬라이드 2 — 연구 질문

**제목:** 연구 질문

**본문(핵심 질문):**
TTS 음성은 인간 음성과 비교했을 때 VOT, f0, 포먼트, 스펙트로그램 구조에서 어떤 차이를 보이는가?

**본문(표 — 비교 항목):**

| 항목 | 단위 | 의미 |
|---|---|---|
| VOT | ms | 자음이 터진 뒤 성대 진동이 시작되기까지의 시간 |
| f0 | Hz | 성대 진동의 기본주파수, 목소리 높낮이 |
| 포먼트 F1/F2 | Hz | 성도 공명으로 생기는 모음의 음향적 위치 |
| 스펙트로그램 | 시간 × 주파수 | 주파수 성분이 시간에 따라 변하는 모습 |

**본문(보조):** Praat은 스펙트로그램, pitch, 포먼트, intensity, jitter, shimmer 등을 분석하는 음성학 도구이다.

**이미지:** 없음.

---

## 슬라이드 3 — 소리는 물리적으로 무엇인가

**제목:** 소리 = 공기의 진동

**본문(불릿):**
- 소리는 공기 압력의 변화가 전달되는 파동이다.
- 마이크는 이 압력 변화를 전기 신호로 바꾼다.
- 컴퓨터는 소리를 시간에 따른 진폭 변화, 즉 파형으로 저장한다.
- 주기와 주파수의 관계: `f = 1 / T` (예: 한 주기가 0.01초이면 f = 100 Hz)

**이미지 [생성]:** sine wave 한 개. x축은 시간, y축은 진폭. 한 주기 구간을 양쪽 화살표로 표시하고 "T (주기)" 라벨을 단다. 단순하고 깔끔하게.

---

## 슬라이드 4 — 인간 음성의 생성 구조

**제목:** 인간 음성 = 성대 진동 + 성도 공명

**본문(표):**

| 단계 | 설명 |
|---|---|
| 성대 | 공기를 주기적으로 끊어 기본 진동을 만든다 |
| 성도 | 입·혀·입술·목구멍이 특정 주파수를 강조한다 |

**본문(보조):** 성대가 소리의 원천(source)을 만들고 성도가 이를 거르는 필터(filter) 역할을 한다. 이를 음원–필터 이론(source–filter model)이라 한다.

**이미지 [준비됨]:** `slide04_source_filter.png` — 음원–필터 합성 도식(성대 배음 스펙트럼 × 성도 필터 곡선 = 출력 스펙트럼).

---

## 슬라이드 5 — f0와 배음

**제목:** f0와 배음: 목소리의 높이와 음색

**본문(불릿):**
- f0: 성대 진동의 기본주파수.
- 배음: f0의 정수배 주파수 성분.

**본문(표 — 예시, f0 = 100 Hz):**

| 성분 | 주파수 |
|---|---|
| 기본주파수 | 100 Hz |
| 2배음 | 200 Hz |
| 3배음 | 300 Hz |
| 4배음 | 400 Hz |

**이미지 [생성]:** 라인 스펙트럼. x축은 주파수(Hz), y축은 진폭. 100, 200, 300, 400 Hz 위치에 세로 막대를 세우고, 막대 높이는 고주파로 갈수록 점차 낮아지게 한다. 각 막대 아래에 "f0", "2f0", "3f0", "4f0" 라벨을 단다.

---

## 슬라이드 6 — 포먼트

**제목:** 포먼트: 모음을 구별하게 하는 공명 주파수

**본문(불릿):**
- 포먼트는 성도에서 특정 주파수 대역이 강하게 나타나는 현상이다.
- /아/, /이/, /우/가 다르게 들리는 것은 입 모양과 혀 위치에 따라 강조되는 주파수 대역이 달라지기 때문이다.

**본문(표):**

| 포먼트 | 주로 관련되는 요소 |
|---|---|
| F1 | 혀의 높이, 입의 벌어짐 |
| F2 | 혀의 앞뒤 위치 |

**이미지 [준비됨]:** `slide06_formant_vowels.png` — /이/와 /아/ 스펙트럼 비교, F1·F2 봉우리 위치 표시.

---

## 슬라이드 7 — 스펙트로그램 읽는 법

**제목:** 스펙트로그램: 소리를 그림으로 보는 방법

**본문(표):**

| 축 / 색 | 의미 |
|---|---|
| x축 | 시간 |
| y축 | 주파수 |
| 어두운 부분 | 에너지가 강한 주파수 대역 |

**본문(보조):** Praat으로 음성을 시간–주파수 관점에서 비교할 수 있다.

**이미지 [준비됨]:** `slide07_spectrogram_labeled.png` — 본인 음성 Praat 캡처(위 파형, 아래 스펙트로그램, 포먼트=빨강, pitch=파랑) + 라벨.

---

## 슬라이드 8 — 한국어 파열음: 바/파/빠

**제목:** 한국어 파열음은 시간 구조가 중요하다

**본문:** 한국어에는 평음·격음·경음의 3중 대립이 있다.

**본문(표):**

| 분류 | 예 | VOT | 뒤 모음 f0 |
|---|---|---|---|
| 평음 | 바·다·가 | 중간 | 낮음 |
| 격음 | 파·타·카 | 김 | 높음 |
| 경음 | 빠·따·까 | 짧음 | 높음 |

**본문(보조):** 한국어 파열음은 VOT와 f0가 함께 단서로 작용하며, 현대 서울 한국어에서는 평음과 격음 구별에서 f0의 중요성이 커졌다는 논의가 있다.

**이미지 [생성]:** VOT 타임라인 도식. 가로축은 시간. 바/파/빠 세 줄을 위아래로 배치하고, 세 줄 모두 같은 위치에 "버스트(파열)" 지점을 세로선으로 정렬한다. 각 줄에서 "성대 진동 시작" 지점을 다르게 두어, 버스트와 진동 시작 사이 구간(=VOT)을 화살표로 표시한다. 경음(빠)은 짧게, 평음(바)은 중간, 격음(파)은 길게 그린다.

---

## 슬라이드 9 — VOT란 무엇인가

**제목:** VOT: 자음이 터지고 성대가 울리기까지의 시간

**본문:** VOT는 파열이 터지는 순간과 뒤따르는 모음에서 성대 진동이 시작되는 순간 사이의 시간이다.

**본문(수식·예시):**
- `VOT = 성대 진동 시작 시각 − 파열 개방 시각`
- 예: 파열 개방 0.520초, 성대 진동 시작 0.565초 → 0.045초 = 45 ms
- 참고 근사 범위: 경음 ~10–20 ms, 평음 ~30–50 ms, 격음 ~70 ms 이상

**이미지 [준비됨]:** `slide09_vot_diagram.png` — 파형 위 버스트 지점·voicing onset 지점 표시, 구간 화살표.

---

## 슬라이드 10 — TTS는 음성을 어떻게 만드는가

**제목:** TTS: 텍스트를 음향 신호로 바꾸는 과정

**본문:** 현대 신경망 TTS는 대체로 텍스트에서 음향 표현을 예측하고, 그 표현을 다시 파형으로 복원한다.

**본문(흐름):**
텍스트 → 발음·음향 특징 예측 → 멜 스펙트로그램 → 보코더 → 음성 파형

**본문(보조):** Tacotron 2는 문자 임베딩을 멜 스펙트로그램으로 예측하고, WaveNet·HiFi-GAN 계열 보코더가 이를 파형으로 합성하는 구조를 제시했다.

**이미지 [생성]:** 세로(또는 가로) 흐름도. 5개 노드를 화살표로 연결: "텍스트" → "발음·음향 특징 예측" → "멜 스펙트로그램" → "보코더" → "음성 파형". 각 노드는 사각형, 사이는 방향 화살표. 깔끔한 단계 도식으로.

---

## 슬라이드 11 — 멜 스펙트로그램을 왜 쓰는가

**제목:** AI는 음성을 주파수 그림으로 다룬다

**본문(불릿):**
- 멜 스펙트로그램은 음성을 시간–주파수 정보로 바꾼 표현이다.
- 인간 청각은 모든 주파수를 똑같이 민감하게 듣지 않으므로, 청각 특성에 맞춘 멜 스케일이 자주 쓰인다.

**이미지 [생성]:** 곡선 그래프. x축은 주파수(Hz), y축은 멜(mel) 값. 저주파에서는 가파르게 오르다가 고주파로 갈수록 완만해지는 로그형 곡선. 축 라벨을 단다.

---

## 슬라이드 12 — 실험 설계

**제목:** 인간 음성과 TTS 음성 비교 실험

**본문(표):**

| 구분 | 내용 |
|---|---|
| 인간 음성 | 본인 또는 2~3명 녹음 |
| TTS 음성 | CLOVA, Google TTS, ElevenLabs 등 중 1~2개 |
| 분석 도구 | Praat |
| 파일 형식 | WAV |
| 반복 | 각 단어 3회 이상 |

**본문(녹음 문장):** "제가 말한 단어는 바입니다." / "…파입니다." / "…빠입니다."

**본문(표 — 분석 단어):**

| 목적 | 단어 |
|---|---|
| 파열음 VOT/f0 | 바, 파, 빠 |
| 추가 파열음 | 다·타·따, 가·카·까 |
| 모음 포먼트 | 비, 베, 배, 바, 버, 보, 부, 브 |
| ㅐ/ㅔ | 배/베, 새/세, 개/게 |

**이미지 [생성]:** 파이프라인 도식. "인간 녹음"과 "TTS 생성" 두 입력이 "Praat 측정"으로 모이고, 거기서 "비교/분석"으로 이어지는 흐름. 노드는 사각형, 연결은 화살표로.

---

## 슬라이드 13 — 비교 1: 바/파/빠의 VOT

**제목:** 인간 음성과 TTS의 VOT 비교

**본문(표 — 측정 후 채움):**

| 음성 | 바 VOT | 파 VOT | 빠 VOT |
|---|---|---|---|
| 인간 | (측정값) | (측정값) | (측정값) |
| TTS | (측정값) | (측정값) | (측정값) |

**이미지 [준비됨]:** `slide13_vot_human.png`, `slide13_vot_tts.png` — 인간 "파" / TTS "파" Praat 캡처 2장(burst·voicing onset 표시, VOT 구간 화살표).

**데이터:** 위 측정값 표는 아래 데이터 객체에서 채운다(표만 데이터 구동, 그래프 없음).

```
// TODO: 실제 측정값으로 교체 (단위: ms)
const votTable = {
  "바": { 인간: 18, TTS: 22 },
  "파": { 인간: 78, TTS: 65 },
  "빠": { 인간: 12, TTS: 15 },
};
```

---

## 슬라이드 14 — 비교 2: f0

**제목:** VOT만으로 충분하지 않다: f0 비교

**본문:** 한국어 평음·격음·경음 구별에서 VOT와 f0가 함께 작용하며, 현대 서울 한국어에서는 f0가 평음·격음 구별의 중요한 단서로 논의된다.

**본문(측정 방법):** 모음 시작 후 약 20~40 ms 지점의 f0, 또는 모음 초반 50 ms 평균 f0.

**본문(표 — 측정 후 채움):**

| 음성 | 바 뒤 f0 | 파 뒤 f0 | 빠 뒤 f0 |
|---|---|---|---|
| 인간 | (측정값) | (측정값) | (측정값) |
| TTS | (측정값) | (측정값) | (측정값) |

**이미지 [데이터 그래프]:** f0 비교 막대그래프. 단어(바·파·빠)별로 인간/TTS 막대를 나란히. y축 f0(Hz). 아래 더미값으로 그리고, 위 측정값 표도 같은 데이터 객체에서 채운다.

```
// TODO: 실제 측정값으로 교체 (단위: Hz, 모음 초반 f0)
const f0 = {
  "바": { 인간: 118, TTS: 125 },   // 평음 → 낮음
  "파": { 인간: 155, TTS: 148 },   // 격음 → 높음
  "빠": { 인간: 150, TTS: 152 },   // 경음 → 높음
};
```

---

## 슬라이드 15 — 비교 3: 모음 포먼트

**제목:** 모음은 F1/F2 좌표로 비교할 수 있다

**본문(표 — 모음과 단어):**

| 모음 | 예시 단어 |
|---|---|
| /i/ | 비 |
| /e/ 또는 /ɛ/ | 베 / 배 |
| /a/ | 바 |
| /ʌ/ | 버 |
| /o/ | 보 |
| /u/ | 부 |
| /ɯ/ | 브 |

**본문(그래프 설명):** x축 F2, y축 F1, 두 축 모두 뒤집어 일반적 모음 공간 형태로 표시. 인간 음성과 TTS 음성을 같은 그래프에 표시.

**본문(보조 — 해석 유의):** 인간과 TTS는 성도 길이가 달라 절대 포먼트값이 다를 수 있으므로, 정규화하거나 "모음 공간의 상대적 배치(모양)"를 비교한다고 밝힌다.

**이미지 [데이터 그래프]:** F1–F2 모음 공간 산점도. x축 F2, y축 F1, **두 축 모두 뒤집어** 표시. 인간/TTS를 같은 그래프에 다른 색으로, 각 점에 모음 라벨. 아래 더미값으로 그린다.

```
// TODO: 실제 측정값으로 교체 (단위: Hz, [F1, F2])
const vowels = {
  "이": { 인간: [290, 2200], TTS: [300, 2150] },
  "에": { 인간: [450, 1900], TTS: [440, 1950] },
  "애": { 인간: [560, 1750], TTS: [570, 1800] },
  "아": { 인간: [750, 1300], TTS: [730, 1350] },
  "어": { 인간: [600, 1050], TTS: [620, 1100] },
  "오": { 인간: [430, 800],  TTS: [440, 850] },
  "우": { 인간: [330, 750],  TTS: [340, 800] },
  "으": { 인간: [350, 1450], TTS: [360, 1400] },
};
```

---

## 슬라이드 16 — 추가 사례: ㅐ/ㅔ 단일화

**제목:** 포먼트로 보는 ㅐ/ㅔ 단일화

**본문:** 모음 포먼트 분석은 모음 구별뿐 아니라 언어 변화도 보여준다. 현대 서울 한국어에서는 ㅐ와 ㅔ의 구별이 약화·합류하는 현상이 보고된다.

**본문(실험 질문):** TTS는 철자가 다른 ㅐ/ㅔ를 음향적으로도 다르게 합성하는가?

**본문(표 — 비교 단어):**

| ㅐ | ㅔ |
|---|---|
| 배 | 베 |
| 새 | 세 |
| 개 | 게 |

**본문(표 — 가능한 해석, 결과 후 적용):**

| 결과 | 해석 |
|---|---|
| 인간·TTS 모두 겹침 | 현대 발화 경향과 유사할 가능성 |
| 인간 겹침 / TTS 구분 | TTS가 철자 차이를 강하게 반영했을 가능성 |
| 인간 구분 / TTS 겹침 | TTS가 개별 화자 발음을 충분히 반영하지 못했을 가능성 |
| 둘 다 구분 | 해당 자료에서 단일화가 강하게 나타나지 않음 |

**이미지 [데이터 그래프]:** ㅐ/ㅔ F1–F2 산점도. 배/베·새/세·개/게 쌍의 F1·F2를 표시해 겹침(합류) 여부가 보이게 한다. x축 F2, y축 F1, 두 축 뒤집기. 인간/TTS 색 구분. 아래 더미값으로 그린다.

```
// TODO: 실제 측정값으로 교체 (단위: Hz, [F1, F2])
const aeE = {
  "배(ㅐ)": { 인간: [560, 1750], TTS: [575, 1720] },
  "베(ㅔ)": { 인간: [470, 1880], TTS: [455, 1960] },
  "새(ㅐ)": { 인간: [555, 1770], TTS: [580, 1700] },
  "세(ㅔ)": { 인간: [465, 1900], TTS: [450, 1970] },
  "개(ㅐ)": { 인간: [565, 1740], TTS: [585, 1710] },
  "게(ㅔ)": { 인간: [475, 1870], TTS: [460, 1950] },
};
```

---

## 슬라이드 17 — 비교 4: 스펙트로그램과 배음 구조

**제목:** 스펙트로그램에서 보이는 인간 음성과 TTS의 차이

**본문(표):**

| 항목 | 볼 부분 |
|---|---|
| 배음 구조 | 일정한 간격의 가로 줄 |
| 고주파 성분 | 마찰음·기식·파열음 주변 |
| 포먼트 | 어두운 에너지 띠 |
| 피치 변화 | pitch contour |

**본문(보조 — 유의):** 차이는 TTS 엔진·음성 설정·압축 방식·파일 형식에 따라 달라질 수 있다. "AI 음성은 무조건 고주파가 부족하다"는 식으로 단정하지 않는다.

**이미지 [준비됨]:** `slide17_spectrogram_compare.png` — 인간/TTS 스펙트로그램 나란히(배음 비교 시 협대역 설정).

---

## 슬라이드 18 — 문장 단위로 확장: 휴지와 억양

**제목:** 단어를 넘어서: 문장 자연성의 문제

**본문(불릿):**
- 짧은 단어에서는 VOT, f0, 포먼트가 중요하다.
- 실제 음성의 자연성은 문장 단위의 휴지·억양·정보구조와도 관련된다.
- 한국어 TTS 연구에서는 문장 내 휴지 형성이 자연스러운 음성에 중요하며, 휴지 오류가 발화 해석과 품질에 영향을 준다고 보고된다.

**이미지 [준비됨]:** `slide18_pitch_contour.png` — 문장 단위 pitch contour(휴지·억양 표시).

---

## 슬라이드 19 — 결론

**제목:** AI 음성의 자연성은 물리적 신호 재현의 문제다

**본문(결론):**
- 인간 음성과 TTS 음성은 모두 파형과 스펙트로그램으로 분석할 수 있는 물리적 신호다.
- 한국어에서는 VOT, f0, 포먼트 같은 음향 단서가 자음과 모음 구별에 중요하다.
- 따라서 AI 음성의 자연성은 "발음을 맞게 읽는가"를 넘어, 한국어의 세밀한 음향 단서를 얼마나 정밀하게 재현하는가의 문제다.

**본문(한계):** 이 발표는 소규모 사례 분석이며, 화자 수가 적어 일반화에는 한계가 있다.

**이미지 [생성]:** 논리 흐름 다이어그램. 6단계를 화살표로 연결: "소리는 파동이다" → "인간 음성은 성대 진동과 성도 공명으로 만들어진다" → "f0·배음·포먼트·VOT로 측정할 수 있다" → "TTS도 최종적으로 음성 파형을 만든다" → "같은 물리적 기준으로 비교할 수 있다" → "한국어에서는 바/파/빠, ㅐ/ㅔ가 좋은 분석 대상이다". 세로 흐름으로.